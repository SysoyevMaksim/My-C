#include <iostream>

using namespace std;

int main()
{
    int n, m, w, x1, y1;
    cin>> n >> m;
    for(int x = 0; x < n; x++)
    {
        for(int y = 0; y < m; y++)
        {
            array [x] [y] = 0;
        }
    }
    cin>> w;
    for(int i = 0; i < w; i++)
    {
        cin>>
    }
    return 0;
}
