#include <iostream>
#include <string>

using namespace std;

int main()
{
    string s, s1, s2;
    getline(cin, s);
    for(int i = 0; i < s.length; i++)
    {
        if(s[i]  )
    }
    return 0;
}
